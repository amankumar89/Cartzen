export interface UserDataProps {
  id?: string;
  name: string;
  email: string;
  imageUrl: string;
}

export interface ProductDataProps {
  id?: string;
  title: string;
  description: string;
  price: number;
  imageUrl: string;
}
